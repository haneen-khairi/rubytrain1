<p style="color: green"><%= notice %></p>

<h1>Jobs</h1>

<div id="jobs">
  <% @jobs.each do |job| %>
  <div class="text-right">
<%= link_to "New job", new_job_path , class:"btn btn-dark"%>
</div>
    <%= render job %>
    <div class="text-center">
    <p>
      <%= link_to "Show this job", job  ,class:" btn  btn-secondary"%>
    </p>
    </div>
  <% end %>
</div>

